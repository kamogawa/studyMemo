1. 집계함수: 복수건의 데이터를 집계하기 위한 함수
  집계 함수를 사용하면 리턴값은 1건만 반환한다.
  집계함수 사용시 개별행의 열값은 참조 불가능 하다.
  where구를 사용하여 조건을 지정한 집계를 할수 있다.
  where구와 group by 구에는 집계함수를 사용할수 없다
  대표적인 집계함수
   - sum(col1): 복수행의 열값을 합산한 결과
   - avg(col1): 평균값
   - max(col1): 최대값 
   - min(col1): 최소값 
     - max min의 경우 문자열 날짜형 데이터에서도 사용가능 
     - max z min a max 최근 min 과거
   - count(col1 or *): 검색된 결과의 행수 , 열을 지정하면 동작이 다름
   - listagg(메이져 열[, 구분자 (디폴트 null)]) within group (order by구)
     - 메이져 열에 대해서 order by구에 지정된 순으로 값을 결합
   
  집계함수에서 null의 취급
   - null은 집계 대상에서 제되된다. 3행중 1개가 null일 경우 avg의 대상은 2개 
   - count 의 경우 열을 지정하면 위와 같이 대상에서 제외 하지만 * 일경우 포함한다. 
   - null 함수를 사용하여 집계에 포함 시킬수 있음
   
  중복 제거 집계 avg(distinct col1)
   - 인수 열명 앞에 distinct를 지정하면 중복을 제거한후 집계를 한다.
   - *는 사용 불가 반드시 열명을 지정해야 한다.
   
  집계 함수에 단일행 함수를 네스트 하여 단일행 함수로 처리한 결과로 집계할 수 있다.
   - select sum(round(con1)) from t1; col1의 반올림 값을 집계
  
2. group by 와 having 
  group by: 집계 함수의 집계는 검색결과에 대한 데이터를 1개의 그룹으로 하여 집계를 실시하기 때문의 전체의 집계만 가능
            데이터의 종류 별로 집계 하고 싶을 경우 group by를 사용한다. 직종별 평균 인원 , 평균 월급 등등
            복수의 열을 지정하여 집계할 열을 그룹화 할수 있다. 계열사의 직종별 등
            집계 함수와 마찬가지로 where조건으로 집계 대상 데이터를 선택한후 집계가 이루어 진다. (집계전)
            where 조건에는 group by와 함꼐 지정된 집계 함수는 사용할 수 없다.
            
  having: where과는 다르계 집계 후 값을 추려내는 방법 (집계후)
          where과는 다르게 group by와 함께 지정된 집계 함수를 사용할수 있다.
          group by 구 앞에 지정 할 수 있다.
          group by 없이 그룹함수를 1개만 지정시 사용할수 있다.
          
  group by 설정 시 집계 함수의 네스트, 집계 함수의 네스트 한 경우 값은 1개
    group by로 부서별 월급 평균을 집계 <- 네스트 된 값, 으로 평균값 집계 (데이터 1개)
    집계 함수의 네스트 사용시 group by의 지정은 필수 1단계(안쪽)값이 1개 밖에 없기 때문에 에러
    집계함수의 네스트는 3개 이상 불가능 2개 까지만 가능 함
    
